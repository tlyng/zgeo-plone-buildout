export LD_LIBRARY_PATH=${libgeos}:${libxml2}:${libxslt}:${libspatialindex}:${libgdal}:${rtree}
export DYLD_LIBRARY_PATH=${libgeos}:${libxml2}:${libxslt}:${libspatialindex}:${libgdal}:${rtree}
export GDAL_DATA=${gdaldata}
